# Отчёт по семестровой работе  
## «Space Invaders на чистом C + Win32 API»

---

# 1. Общая информация

Проект представляет собой 2D-игру **Space Invaders**, реализованную на:

- Язык: **C (C11)**
- API: **чистый Win32 API**
- Графика: **GDI**
- Без использования C++, STL, SDL, SFML, OpenGL и т.п.

Игра содержит:

- 10 уровней с нарастающей сложностью
- Несколько режимов стрельбы врагов
- Систему случайных усилений (power-up)
- Сохранение хай-скора
- Двойную буферизацию (без мерцания)

---

# 2. Архитектура проекта

Проект строго разделён на независимые модули.

## 2.1 Модуль `core / logic`

Файлы:
- `game.h / game.c`
- `levels.h / levels.c`
- `entities.h`
- `powerup.h`

Отвечает за:
- Игровое состояние (`GameState`)
- Движение объектов
- Коллизии
- Логику уровней
- AI врагов
- Таймеры
- Систему усилений
- Начисление очков

Важно:  
В логике **нет HWND, HDC, GDI-объектов**.

---

## 2.2 Модуль `ui / render`

Файлы:
- `render.h / render.c`

Отвечает за:
- Отрисовку через GDI
- Двойную буферизацию
- Рисование спрайтов программно
- Отображение HUD
- Меню
- Отображение активных усилений

Все GDI-ресурсы создаются в `Render_Init()`  
и освобождаются в `Render_Destroy()` через `DeleteObject`.

Утечек GDI-объектов нет.

---

## 2.3 Модуль `input`

Файлы:
- `input.h / input.c`

Отвечает за:
- Плавное движение (`GetAsyncKeyState`)
- Одиночные действия (`WM_KEYDOWN`)

---

## 2.4 Точка входа

Файл:
- `main.c`

Отвечает за:
- Создание окна
- Игровой цикл
- Связь между логикой и рендером

---

# 3. Игровой цикл

Используется фиксированный шаг:

```c
FIXED_DT = 1.0f / 60.0f;
```

С накоплением времени:

```c
while (accumulator >= FIXED_DT) {
    Game_Update(&g_state, FIXED_DT);
    accumulator -= FIXED_DT;
}
```

Время рассчитывается через:

```c
QueryPerformanceCounter
```

Sleep для главного цикла **не используется**.

---

# 4. Структура состояния игры

Вся игра хранится в структуре:

```c
typedef struct GameState
```

Она содержит:

- Игрока
- Пришельцев
- Пули
- Барьеры
- UFO
- Power-up
- Активные эффекты
- Таймеры
- Очки
- Текущий уровень

GameState передаётся по указателю между модулями.

---

# 5. Система уровней

В `levels.c` реализовано 10 уровней.

Усложнение происходит за счёт:

- Увеличения базовой скорости врагов
- Ускорения пуль
- Уменьшения интервала стрельбы
- Увеличения скорости поворота homing-пуль
- Увеличения количества врагов

## Режимы стрельбы врагов

1. `SHOOT_STRAIGHT` — прямо вниз
2. `SHOOT_AIMED` — в текущую позицию игрока
3. `SHOOT_HOMING_SLOW` — медленное самонаведение

Homing реализован через плавный поворот угла:

```c
diff = targetAngle - currentAngle;
diff = clamp(diff, -turnRate*dt, +turnRate*dt);
```

---

# 6. Система усилений (Power-Up)

Добавлена расширенная система бонусов.

## 6.1 Механика

После убийства врага:

```c
if (rand() % 100 < PU_DROP_CHANCE)
```

создаётся падающий шар усиления.

Игрок должен поймать его.

## 6.2 Типы усилений

| Тип | Обозначение | Эффект |
|------|-------------|--------|
| Extra Life | +1 | +1 жизнь (навсегда) |
| Shield | SH | Щит на 12 секунд |
| Slow | SL | Замедление врагов |
| Fast Shoot | FS | Ускоренная стрельба |
| Triple Shot | 3X | Тройной выстрел |
| Bomb | BM | Взрыв при попадании |

## 6.3 Реализация

Структура:

```c
typedef struct ActiveEffects
```

Каждый эффект имеет таймер.

При окончании таймера параметры возвращаются к базовым значениям.

---

# 7. Коллизии

Используется AABB (Axis-Aligned Bounding Box):

```c
rectsOverlap(...)
```

Проверяются:

- Пули игрока ↔ враги
- Пули игрока ↔ UFO
- Пули игрока ↔ барьеры
- Пули врагов ↔ игрок
- Пули врагов ↔ барьеры
- Пришельцы ↔ барьеры
- Power-up ↔ игрок

---

# 8. Двойная буферизация

Используется:

```c
CreateCompatibleDC
CreateCompatibleBitmap
BitBlt
```

WM_ERASEBKGND подавляется:

```c
return 1;
```

Мерцания нет.

---

# 9. Управление памятью

- Динамическая память не используется.
- Все массивы статические.
- Все GDI-объекты освобождаются.
- Файл `hiscore.dat` закрывается после использования.
- Используется `snprintf` вместо `sprintf`.

Утечек памяти нет.

---

# 10. Балансировка

Для компенсации высокой сложности уровней 7–10 добавлена система усилений.

Также реализованы:

- Ограничение скорости поворота homing-пуль
- Возможность накопления жизней без ограничения
- Замедление врагов
- Временная неуязвимость

Игра стала сложной, но проходимой.

---

# 11. Соответствие требованиям задания

| Требование | Выполнено |
|------------|-----------|
| Строго C | Да |
| Только Win32 API | Да |
| Нет C++ | Да |
| Нет глобальных переменных | Да |
| Разделение на модули | Да |
| Двойная буферизация | Да |
| Фиксированный шаг | Да |
| Освобождение GDI | Да |
| Собирается из коробки | Да |

---

# 12. Итог

Разработана полностью функционирующая 2D-игра на чистом C и Win32 API с:

- модульной архитектурой
- фиксированным игровым циклом
- системой усилений
- процедурной прогрессией сложности
- сохранением прогресса
- отсутствием утечек памяти
- корректным управлением GDI-ресурсами

Проект соответствует требованиям семестровой работы.